node() {
    
    def body = '{"name": "hello", "age": 30 }'
    toJson(body)
}



def toJson(responseBody) {
	def parser = new groovy.json.JsonSlurper()
	def body = parser.parseText(responseBody)
    println(body)
}
